# Compiled output / build artifacts
/target/

# Generated by install.sh (double-clickable launcher with machine-specific paths)
/LocalAgent.app/

# Internal notes — not part of the public project
/EXPERIMENTS.md
/DISTRIBUTION.md

# Cargo lockfile: keep for binaries, ignore for libraries.
# This is a binary, so you may COMMIT Cargo.lock instead — delete the next line if so.
# Cargo.lock

# Backtrace / debug
*.pdb

# Editor / OS noise
.DS_Store
*.swp
.idea/
.vscode/*
!.vscode/settings.json
